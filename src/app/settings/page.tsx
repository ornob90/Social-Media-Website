import AccountSettings from "@/components/ui/settings/AccountSettings/AccountSettings";
import PostSettings from "@/components/ui/settings/PostSettings/PostSettings";
import React from "react";

const Settings = () => {
  return (
    <section>
      <AccountSettings />
      <PostSettings />
    </section>
  );
};

export default Settings;
