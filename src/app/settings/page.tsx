import AccountSettings from "@/components/ui/settings/AccountSettings/AccountSettings";
import React from "react";

const Settings = () => {
  return (
    <section>
      <AccountSettings />
    </section>
  );
};

export default Settings;
